language: node_js
node_js:
  - 9
cache:
  directories:
    - node_modules
script:
  - npm run build
  - npm test --coverage

after_script:
   - COVERALLS_REPO_TOKEN=$coveralls_repo_token npm run coveralls
